#pragma once
#include <stdbool.h>

bool CheckLength(const long long len);
bool CheckInput(const int countVertices, const int countEdges);
bool CheckVertex(const int vertex, const int maxVertexNumber);
